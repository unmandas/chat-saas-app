import React from "react";
import Logo from "./Logo";
import { Button } from "./ui/button";

const Header = () => {
  return (
    <header>
      <nav>
        <Logo />
      </nav>
    </header>
  );
};

export default Header;
